<%- include('../includes/head.ejs') %>
</head>

<body>
    <%- include('../includes/navigation.ejs') %>
<main class="centered">
    <h1><%= product.title %></h1>
    <hr>
    <div>
    <img src="<%= product.imageUrl %>" alt="<%= product.title %>" />
    </div>  
    <h2>$<%= product.price %></h2>
    <p><%= product.description %></p>
    <%- include('../includes/add-to-cart.ejs', {product: product}) %>
    <!-- when using a loop and include inside it, you need to pass the local variable
    as a second argument to the includes() function as above.-->
</main>

<%- include('../includes/end.ejs') %>